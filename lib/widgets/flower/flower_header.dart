import 'package:flutter/material.dart';

class FlowerHeaderView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        ListTile(
          leading: image,
        )
      ],
    );
  }
}
